import React from "react";
import styles from "./List.module.css";

const List = ({ list }) => {
  return (
    <div className={styles.detail} style={{ backgroundColor: "black" }}></div>
  );
};

export default List;
